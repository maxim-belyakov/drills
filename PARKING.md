# Parking

Everything that surfaced during the cycle and asks to be added to the programme.
**Do not touch until the cycle is over.** The next cycle is built from this list.

- Narrating out loud while typing does not happen by itself yet (2026-08-03, drill 01).
  Explaining clearly under load is a repeat interview failure - Coralogix, Wolters Kluwer, DPG Media.
- `=` typed instead of `===` inside a predicate (2026-08-04, drill 02). Caught by eye both times, but in a callback it is a silent bug: `item.active = true` assigns, mutates the object and returns truthy, so filter keeps everything. Worth one dedicated trace-prediction drill in cycle 2.
- `some` / `every` were not recallable on the first ask (2026-08-04). Empty-array edge (`[].every` is true) is the part to re-test in a week.
- Tie-breaking is not part of the reflex yet (2026-08-05, drill 03). Every find-the-max has a behaviour on equal values, and it has to be said out loud before the interviewer asks. Same family: sort stability, first vs last match.
- Accumulator on the left is a habit, not a rule to rediscover. Flipping it is harmless for `+` on numbers and a real bug for string concat or `push`.
- **Discarded expressions** (2026-08-06, drill 04, second sighting). Wrote `acc[k] + 1` with no assignment - computes the answer and throws it away. Same family as `=` for `===` in drill 02: no syntax error, no crash, wrong result. Two sightings in four days makes it a class, not a slip. Cycle 2: a trace-prediction drill built entirely of statements that look like work and do nothing.
- **The reduce initial value is known but not reflexive** (2026-08-06). Said the rule out loud on 08-05 in drill 03, dropped `, {}` in all four functions on 08-06. Knowing the rule and having the reflex are different states - re-test in a week, not by asking, by writing.
- **Tests that are green for the wrong reason** (2026-08-08, drill 05, THIRD sighting). `greet` ignored its parameter and read a module-level global; the check passed only because the same object was passed in. Prior sightings: DPG Media rejection (tests exercised the original class, not the refactor) and the Next.js `[id]` route test importing the neighbouring module. Cycle 2 drill: given a passing test, prove it can fail - change the input and watch it go red. A green test that cannot go red asserts nothing.
- **Deep copy vs structural sharing** (2026-08-09, drill 06, spoken part). Knew spread is shallow, could not name what to reach for when a real deep copy is needed, or its cost. The interview-grade answer is not `structuredClone` but copying along the changed path only. Cycle 2: a drill on nested immutable updates, two and three levels deep.
- **Shadowed names** (2026-08-09, drill 06). `function sum(...sum)` with `sum` again as the reduce accumulator - three different things with one name in nested scopes. Works, reads badly, and is the kind of thing an interviewer stops to comment on.
- **Implicit globals** (2026-08-10, drill 07). `a = true` inside a catch, with `a` never declared, silently creates a property on the global object in sloppy mode and throws `ReferenceError` in a module or under "use strict". Worth one drill in cycle 2 on what strict mode actually changes - this, `this` binding, duplicate parameters, octal literals.
- **State the rule, then break it 40 lines later** (2026-08-10, drill 07). Wrote `"" ?? x` vs `"" || x` correctly in task 3 and used `||` for the fallback in task 1 of the same file. Second sighting of this exact shape after the missing reduce seed on 08-06. The gap is not knowledge, it is the hand under load.
- **Tuning the wrong knob** (2026-08-12, drill 08). `sort((a, b) => a - b)` on objects returns NaN, which sort silently treats as "equal" - no crash, no sorting. Three attempts in a row varied the OPERATOR (`a - b`, `b - a`, `a + b`) while the actual fault was the OPERANDS (whole objects instead of `.price`). Cycle 2 drill: when output is wrong, log the value flowing through before changing the shape of the expression. One `console.log` inside the comparator ends this in five seconds.
- **Accumulator shape is not consolidated** (2026-08-12, spaced repeat 04). Repeats of 01, 02, 03 were green on the first try; 04 took five. The drills where the accumulator is a number or an element have stuck; the one where it is a growing structure has not. Bring 04 back sooner than the weekly slot.

- **The `for` family is not known** (2026-08-13, drill 09, said out loud by Maksim). `for...of` versus `for...in` versus the classic index loop versus `forEach` - which one yields values, which yields keys, which can `break`, which one is wrong on arrays. Cycle 2 drill.
- **Mutating versus non-mutating array methods** (2026-08-13, drill 09). Called `list.filter(...)` and returned `list`, having confused it with `sort` - "filter must be mutating too". The split (`sort`, `reverse`, `splice`, `push`, `pop`, `shift`, `unshift`, `fill`, `copyWithin` mutate; everything else returns a new array) is a single card and a single drill.
- **Using map for side effects** (2026-08-13, drill 09). `list.map(item => { ... })` with the result discarded, twice in one sitting. Already card 01 of the new deck - which means the card exists and the hand does not have it yet.
- **Copying an example together with its sample values** (2026-08-18, drill 11). `ok(value)` resolved with the literal `'hi'` from the example and `fail(message)` rejected with `'boom'` - the parameters sat unused. The checks passed because they used exactly those values. Eighth sighting of the wider class. Cycle 2: a drill where every check passes a value the example never mentioned.

- **`instanceof` and the prototype chain** (2026-08-19, asked directly). Does not know what `instanceof` is. Sits inside the wider prototype topic already parked, and is the practical entry point to it. Cycle 2.
- **Unhandled rejections** (2026-08-19, drill 12). Left one escaping twice: once by skipping the handler, once from `forEach` with an async callback, which discards the promises it creates. Node has exited the process on this since v15 - in a service that is an outage. Cycle 2: a drill on where rejections go when nobody is holding the promise.
- **Dead code after return** (2026-08-19, drill 12). Put the fix after `return` in both branches and could not see why nothing changed. Cheap to catch with a linter rule; worth one trace-prediction card.
- **The promise combinators take an ITERABLE, not loose arguments** (2026-08-20, drill 13). Wrote `Promise.all(ok(1), ok(2))` twice before looking it up. Same for `race`, `any`, `allSettled` - one array in. Cheap card, and it is the kind of thing that stalls a live round for a minute.
- **`Promise.all` accepts non-promises** (2026-08-21, drill 14). `Promise.all([1,2,3,4])` resolves instantly with the plain values - no work done, no error raised. The drill passed on it because the expected values matched. Anything not thenable is simply wrapped, which makes a forgotten `.map(fn)` invisible. Cycle 2: a timing assertion belongs on every parallelism drill.

- **`push` returns the new length, not the array.** `acc[k] = (acc[k] ?? []).push(x)` quietly stores a number. Same family: `sort`/`reverse`/`splice` return the array itself (mutated), `map`/`filter`/`slice`/`concat` return a new one. Know what each hands back before chaining onto it. (2026-08-26, assembly A2)
- **Every plain-object key is a string.** `obj[null]` becomes the key `"null"`, `obj[1]` becomes `"1"`, `obj[{}]` becomes `"[object Object]"`. Nothing throws. A `null` field folded into a counter object creates a phantom entry that can outrank real ones. `Map` keeps key types as they are - that is the difference that matters. (2026-08-26, assembly A2)
- **When the error message does not match your understanding, suspect the spelling first.** `localCompare` for `localeCompare`, a missing `.` before `sort`. Both read as "this concept does not work" and both cost most of a session. (2026-08-26)
- **What follows `await` is a MICROtask, not a macrotask.** `await` is sugar over `.then`, so the continuation is queued exactly like a `.then` callback and runs before any timer, even a timer scheduled earlier. Only timers, intervals and I/O are macrotasks. Sighted 2026-08-28, where the wrong mechanism happened to give the right order because the snippet had no timers.
- **`const` and `let` are not hoisted the way `function` is.** The name exists from the top of the block but cannot be read until its declaration line - the temporal dead zone. Writing `onClick={handleClick}` above `const handleClick = ...` is a ReferenceError at render, not a warning. (2026-09-01, drill 17)
- **Optional chaining used as a bandage.** `tags?.map` stopped a crash, but the crash was telling the truth: state had briefly become `undefined` because a setter was nested inside its own updater. `?.` belongs on fields that may legitimately be absent; reach for it where a value is supposed to exist and you have hidden the real fault. (2026-09-01, drill 17)
- **The error message usually contains the answer.** `nothing matches #count` plus the current HTML said exactly what was missing, and the guess went to "the first render beats the prop" instead. Same failure as the live round on 2026-08-25. Read the message to the end before forming a hypothesis. (2026-09-01)

- **`useEffect` with no dependency array re-runs after every render, including the render its own `setState` caused.** That makes a loop: effect -> setState -> render -> effect. It stops only if the new state is `Object.is`-equal to the old one and React bails out - which is luck, not design. Symptom in this harness: a green check plus an "update was not wrapped in act(...)" warning, meaning something fired after the check stopped watching. Read that warning as a leak, not as noise. (2026-09-02, drill 18)
- **A closure keeps the BOX, not the value.** How many boxes exist is decided by how many times the declaration actually runs: `let` above a loop is one box shared by every iteration, `let` in the `for` header is a fresh box per iteration, `let` in the loop body is a fresh box per iteration, `var` is one box per function call regardless of blocks. The stale-response guard (`let current = true` inside the effect) works only because each effect run is a new function call and therefore a new box. (2026-09-02, drill 18, named by Maksim as a gap)
- **`await` on a value that is not a promise still costs a microtask hop.** `await runSearch(q)` where `runSearch` returns a string works, but defers the `setState` by a tick, and the `try/catch` wrapped around it catches nothing that could ever happen. Await what actually returns a promise. (2026-09-02, drill 18)

- **Sorting a prop array in place is a silent data bug, not a style issue.** Order is often data (join order, arrival order, insertion order), and `list.sort()` destroys it for every other reader. In React it is worse three ways: the array belongs to the parent, another component may render the same array with a different order, and mutation does not change the reference - so `Object.is(before, after)` stays `true` and the re-render may not happen at all. Sort in place only an array you created yourself on that line. (2026-09-03, drill 19)
- **A comparator can have a block body.** `sort((a, b) => { if (by === "name") return a.name.localeCompare(b.name); return b.score - a.score; })` is a complete, correct answer; the ternary is only a shortening. Sighted as a blocker: he had written both comparators separately in earlier drills but had never seen one chosen between, and the one-line form read as a new construct. When an arrow grows past one expression, braces and `return`. (2026-09-03, drill 19)
- **Read the given code before writing against it.** `person={item.name}` where `Row` reads `person.name`; the rendered html said `id="b-undefined"`. Protocol: for every component you are handed, write down its name, its props, what it reads off each prop, and what it renders - four lines, one minute - before the first line of your own code. (2026-09-03, drill 19)

- **Never default a boolean with `||`.** `useState(initial || true)` returns `true` for every input, because `false` is exactly what `||` replaces. The prop is silently ignored and the bug only shows when someone passes `false`. Use the value alone, or `??` when a default is genuinely needed. (2026-09-04, drill 20)
- **`.trim()` inside `onChange` makes spaces untypable.** It runs on every keystroke, so a trailing space is stripped before it can become a leading gap between two words: typing "John Smith" character by character yields "johnsmith". Trim on submit; during typing only normalise what can never be valid mid-word, such as case. (2026-09-04, drill 20)
- **Write the boundary the spec states, not one that happens to match the test value.** "100 and above" is `>= 100`; `> 99` passes a check that feeds exactly 100 and calls 99.5 boiling. Equivalent on the integers you were shown, wrong on the first fractional input. Same trap he successfully FOUND on 2026-09-01 when it was planted for him. (2026-09-04, drill 20)
- **A task enters the microtask queue at the moment the promise is already settled.** `await Promise.resolve()` queues its continuation there and then, so it runs before a `.then` written on a later line; `.then` on a still-pending promise queues nothing until that promise settles. Both halves have now been missed once each, in opposite directions (2026-09-03 and 2026-09-04).

- **Derived data is computed during render, not stored in state.** `useState` + `useEffect(() => setVisible(filtered), [q, flag])` for something that is a pure function of props and state buys two bugs and no benefit: the prop the effect reads is usually missing from the dependency array, so a new prop is ignored forever, and the first render pass paints the initial value - typically an empty list plus its "nothing found" message - before the effect runs. Both measured on 2026-09-05: a board handed a new `tasks` array kept rendering the old ids, and render pass 1 showed the empty state while pass 2 showed the list. Compute it as a `const` in the body; reach for state only for what a user action changes.
- **A checkbox controlled with `value` instead of `checked` is not controlled at all.** It appears to work because a click toggles the DOM natively and `e.target.checked` still reports the new value. It breaks the moment anything sets the state without a click: measured on 2026-09-05, a Clear button left the box ticked while state said `false`. (timed build 3)

- **`indexOf` returns a number, not a boolean.** `-1` when the element is absent, the index otherwise - so `if (arr.indexOf(x))` is wrong in BOTH directions: it is false for an element sitting at index 0, and true for an element that is not there at all. Use `includes` to ask whether something is present; use `indexOf` only when you need the position, and compare explicitly against `-1`. (2026-09-07)
- **`reduce` with no initial value is legal, and throws only on an empty array.** On a non-empty array it takes element 0 as the accumulator and starts at index 1. On an empty one it is `TypeError: Reduce of empty array with no initial value` - the case that reaches production first, because empty lists are normal. Writing `reduce` without an initial value is a claim that the array can never be empty. (2026-09-07)
- **`React.memo` is dead code unless every object, array and function prop is reference-stable.** It compares props shallowly with `Object.is`, so one inline `{...}`, `[...]` or `() => {}` in the parent defeats it completely - measured 4 renders instead of 1. memo at the child and useMemo/useCallback at the parent are one technique, not two; miss one prop and the whole thing buys nothing but a comparison on every render. (2026-09-07, drill 21)
- **`useCallback(fn, deps)` is exactly `useMemo(() => fn, deps)`.** useMemo calls what you hand it and caches the result; useCallback caches the function itself. Worth being able to say in one line - it is a standard interview follow-up. (2026-09-07, drill 21)

- **The falsy list is closed and has eight members:** `false`, `0`, `-0`, `0n`, `""`, `null`, `undefined`, `NaN`. Everything else is truthy, including `"0"`, `"false"`, `" "`, `[]`, `{}`. Two consequences that bite in the same component: `if (data)` lets an empty array through and renders a list of nothing, while `if (data.length)` correctly reads 0 as false. (2026-09-08, drill 22)
- **`useEffect(async () => ...)` breaks cleanup, and in React 19 it throws.** An async function always returns a promise, and React calls whatever the effect returned as the cleanup - `TypeError: destroy is not a function` on unmount. The `return () => {...}` written inside an async effect is not a cleanup at all, it is a value buried in a promise, so a stale-response guard built that way fails silently. Declare an async function inside the effect and call it; keep the cleanup return at the top level. (2026-09-08, drill 22)
- **"One state object avoids extra renders" is the wrong argument.** React 18 and 19 batch updates inside promise callbacks too - three setStates in a `.then` produce one render, measured. The real argument is that three separate cells let you write a state that cannot exist: a fresh success sitting next to a stale `error`, because only two of the three lines were written. One object forces every transition to state every field. (2026-09-08, drill 22)

- **`=` inside an `if` is a condition that is always true.** `if (e.name = 'AbortError')` assigns and evaluates to the assigned string, so every error takes the branch - and the object is mutated on the way, so `e.name` downstream reads a value nobody set deliberately. Checks rarely catch it, because a branch is normally only exercised on the input it was written for. Read every `if` twice: once for what it does, once for what input takes it that should not. (2026-09-08, drill 15 re-run)
- **Do not return an Error as if it were a result.** `catch (e) { return e; }` hands an `Error` back in the position where the caller expects data, and nothing announces it: `const data = await f()` then reads properties off an error object and fails three layers from the cause. Either `throw e`, or wrap it deliberately as `{ ok: false, error }`. (2026-09-08, drill 15 re-run)

- **`useCallback(fn)` with no dependency array caches nothing.** Measured: a new function on every render, so the wrapper is pure overhead and any `memo` downstream is defeated. The second argument is not optional the way it is for `useEffect` - without it the hook has no way to know when to reuse. (2026-09-09, drill 23)
- **`setTimeout(fn, 0)` is a MINIMUM delay, not an exact one.** It means "not before 0 ms, and only when this macrotask's turn comes" - if the current task runs for 200 ms the timer fires after 200 ms. Browsers additionally clamp nested timers to 4 ms after five levels, so a chain of zero-delay timers walks at 4 ms a step. (2026-09-09, drill 16 re-run; the half he has now missed twice)
- **A debounced value and the value the field shows are two different things.** The input binds to the immediate state so it repaints on every keystroke; only the work behind it reads the settled one. Bind the field to the debounced value and you have built the laggy input the technique exists to prevent - measured as an empty field while typing. (2026-09-09, drill 23)

- **`useState(slowInit())` calls the function on EVERY render.** The argument is evaluated before `useState` runs, so React receives a value it then ignores from the second render on. Measured over five re-renders: `useState(slowInit())` called it 6 times, `useState(slowInit)` called it once. Passing the function itself is lazy initial state - React invokes it only on mount. Both show the right value on screen, which is why the expensive form survives review. (2026-09-10, drill 17 re-run)
- **React decides whether to re-render by comparing the new state with the old using `Object.is`.** Mutating an array and handing back the same reference compares equal, so nothing re-renders: measured, the counter stayed at 1 after a push, and became 2 only when a new array was passed. `Object.is(a, a-after-push)` is `true`. Replace, never mutate. (2026-09-10, drill 17 re-run)
- **Two `setCount(count + 1)` in one handler add one, not two.** `count` is a constant of that render - it does not change between the two lines, so both compute the same value and the second write overwrites the first. Nothing to do with batching being avoidable: the updater form `setCount(prev => prev + 1)` reads the pending value from React instead of from the closure and adds two. (2026-09-10, drill 17 re-run)

- **The cleanup runs at TWO moments, and the one people forget is the important one.** Before every re-run of that same effect, and on unmount. Measured with an effect on `[id]`: rerender with the same id logs nothing, rerender with a new id logs `CLEANUP for id=1` then `EFFECT for id=2`, unmount logs `CLEANUP for id=2`. React never lets two runs of one effect be open at once - it closes the old before opening the new. That first moment is what makes a debounce cancel and a stale-answer guard flip; describing the cleanup as "runs on unmount" removes the half that does the work. (2026-09-11, drill 18 re-run)
- **`clearTimeout` really does stop a `setInterval`.** Timeout and interval ids share one space in browsers and in Node, so the mismatched cleanup is a readability bug, not a leak - measured: zero ticks after unmount. Worth knowing because it means a mismatched pair will never be caught by a test, only by a reader. (2026-09-11, drill 18 re-run)
- **Adding the state to the dependency array is not a fix for a stale closure, it is churn.** `setCount(count + 1)` inside an interval with `[count]` restarts the interval on every change - measured: 2 intervals started and 2 cleanups run in 35 ms, and the clock restarts from zero each time so it drifts. The shape to recognise is **an effect that depends on what it writes**. The updater form removes the read, and the dependency disappears from the array on its own. (2026-09-11, drill 18 re-run)
- **A stale answer is refused, not cancelled.** A promise cannot be un-awaited. The cleanup flips a flag that belongs to its own run of the effect; when the slow answer finally arrives its `.then` reads that run's flag, sees `false`, and declines to set state - measured: answer for id 1 arrives after id 2 and drops itself. A loading state has nothing to do with it. For a real `fetch`, `AbortController` is the stronger version because it actually stops the request. (2026-09-11, drill 18 re-run)
- **A chained `.then` is not queued at synchronous time.** `Promise.resolve().then(C).then(D)` puts only C in the microtask queue during the synchronous run; D is attached to the pending promise that `.then(C)` returned and is queued only when C finishes. So `queueMicrotask(E)` written after both still runs before D: the order is C, E, D. A chain is a chain of promises, one microtask tick per link, not a list of jobs. (2026-09-11, opener snippet)

- **Right diagnosis, wrong treatment: lifting state where a key was missing.** "The row unmounts when it leaves the list, so its state goes with it" is true under `key={index}` and is not a reason to lift the state - it is the symptom of the key. React decides which row stays mounted by key alone; with the item's id as the key the row survives the filter and keeps its `useState`. Lift state only when someone OTHER than the row needs to read it. Third framing of "a key is identity, not uniqueness" (09-03 twice, 09-14). (2026-09-14, timed build 4)
- **A threshold rewritten to the test's value is still a threshold.** `q.length > 2` became `q.length >= 2` because the failing check typed "qa". The spec said "any non-empty query"; the line that says that is `if (q && ...)`. When a fix is a number that matches the check's input, the fix is wrong by construction. (2026-09-14, timed build 4, tenth sighting of the second finding)
- **`JSON.stringify`: an object DROPS the key, an array KEEPS the slot as `null`.** `{ a: undefined, b: () => 1 }` serialises to `{}`; `[undefined, () => 1]` serialises to `[null,null]`, length kept. Missed on 09-10, 09-11 and 09-14 in three different framings; a card exists and the hand still does not have it. (2026-09-14, opener)

- **A re-render is not a remount, and confusing them hides what memo actually costs.** A new function or object identity makes a `memo` child fail its comparison and **render again** - it does not unmount it. Measured on `StepPanel` with three clicks: `useCallback(..., [])` gives 1 render, 1 mount, 0 unmounts; `useCallback(() => setCount(count + 1), [count])` gives **4 renders, still 1 mount, still 0 unmounts**, and the number on screen is 3 either way. Unmount and remount is what a changed **key** does, and that is a different mechanism entirely. (2026-09-15, drill 21 re-run)
- **Which hook to reach for is decided by what goes into the prop.** An object or array prop means `useMemo`; a function prop means `useCallback`. They are one mechanism - a cache keyed by dependencies - differing only in what is cached: `useMemo` keeps the result of the call, `useCallback` keeps the function. There is a second, separate reason to use `useMemo` that has nothing to do with props: an expensive computation you do not want re-run on unrelated state. (2026-09-15, drill 21 re-run)
- **`JSON.stringify` adds the quotes, `JSON.parse` removes them.** `JSON.stringify("hi")` is four characters and `.length` proves it; `JSON.parse('"hi"')` is two. The quotes are the format's markup, not the data. Separately, `String(obj)` is the literal text `[object Object]` - square brackets, lower-case first word. (2026-09-15, opener snippet)
- **`Object.is` never looks inside.** `Object.is({x:1}, {x:1})` is `false` and `Object.is(a, a)` is `true`; a spread copy is a new box holding the same things, so `Object.is(arr, [...arr])` is `false` while `arr[0] === copy[0]` is `true`. This is the comparison `React.memo` runs on every prop, which is why an inline literal in JSX defeats it. (2026-09-15, opener snippet)

- **A module constant does not belong in a dependency array.** `useMemo(() => slowScan(TRACKS, genre, rating), [TRACKS, genre, rating])` is harmless by result and wrong by reasoning: `TRACKS` is defined at module level, it never arrives from a render and its reference never changes. Only values the body takes FROM THIS RENDER go in - props, state, and functions defined inside the component. Setters from `useState` and anything global are already stable. (2026-09-16, drill 21 third pass)
- **A controlled number field stores a string unless you convert it.** `useState(0)` then `onChange={(e) => setRating(e.target.value)}` starts as a number and becomes a string on the first keystroke. Comparisons hide it - `5 >= "4"` is `true`, because `>=` coerces when one side is a number - so a check that only compares stays green. Arithmetic is where it surfaces: `"4" + 1` is `"41"`. Convert at the edge: `setRating(Number(e.target.value))`. (2026-09-16, drill 21 third pass)
- **`Object.is` is `===` with two fixes, and nothing else.** `Object.is(NaN, NaN)` is `true` where `===` says false; `Object.is(0, -0)` is `false` where `===` says true. Everything else is identical, which means it never looks inside an object: `slice`, `filter`, `map` and spread all return something new, so all of them compare `false` against the original while `arr[0] === copy[0]` stays `true`. This is the comparison React runs before agreeing to re-render, and the reason an inline literal in JSX defeats `memo`. (2026-09-16, missed on two consecutive days)
- **A re-run whose reference answer is one tap away is not a re-run.** On 09-15 drill 21 came back green because the file was open on the phone. The fix was not a rule about honesty but a change of material: the third pass was rebuilt on new data with no reference answer in the repository, and it closed cold the next day. When a drill returns for the third time, rebuild it rather than reset it. (2026-09-16)

- **The queue holds numbers in one form and instructions in the other.** Three `setTotal(total + 5)` in a row put `[5, 5, 5]` in the queue, because `total` is a const of that render and all three expressions were computed before React saw any of them; the last write wins and the total is 5. Three `setTotal(p => p + 5)` put three functions in the queue and React feeds each result into the next: `[0, 5, 10]`, total 15. Measured, and in BOTH cases there is exactly one re-render - batching decides how often the screen is painted, the form of the call decides what number lands. (2026-09-17, drill 17 third pass)
- **An index inside a key is still an index.** `key={`${item}-${index}`}` looks like a fix for duplicate content and is not one: the key changes on every shift of the list. Measured with three rows each holding their own state - a note typed into row 2 shows up on row 3 after row 1 is deleted, while `key={item.id}` keeps it in place. Identity is issued when the item is created and never derived from its content or its position. (2026-09-17, drill 17 third pass)
- **`setTimeout` returns an id, `push` returns a length.** Neither returns what the name suggests: `const t = setTimeout(fn, 25); t();` throws because `t` is a number, and `setItems(prev => prev.push(x))` puts a number into state, so the next `.map` throws. Before using the result of a built-in, say out loud what it returns. (2026-09-17, his own diagnosis)
- **Reading state right after the setter shows the old value.** `setX(v); console.log(x);` prints the previous one - `x` is a const of the current render, not a live view of the store. The new value exists only in the next render. This is the same const that makes `setTotal(total + 5)` add five once. (2026-09-17, his own diagnosis)
- **A controlled input cannot be told from an uncontrolled one by reading its value.** Both show what was typed, because typing writes the DOM property directly. What separates them is the `value` ATTRIBUTE: React sets it on a controlled field and leaves it absent otherwise. A check that only reads `.value` passes an uncontrolled input - fixed in the drill 17 third pass. (2026-09-17)
- **Asked for, not yet built:** drills on the return values of built-in functions, immutable array updates in state, the asynchrony of the setter, and `act()` with timers in tests. His own list of weak spots, 2026-09-17. Candidates for the next cycle.
